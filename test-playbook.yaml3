---

- name: run task on node
  hosts: node
  tasks:
   - name: creating a multiple directory in a loop
     file:
       path: "$HOME/{{ item }}"
       state: directory
     loop:
       - test_directory1
       - test_directory2
       - test_directory3