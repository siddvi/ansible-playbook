---

- name: run task on node
  hosts: node
  tasks:
   - name: ensure a file exits
     file:
       path: $HOME/test_file
       state: touch

   - name: creat symlink to test
     file:
       scr: $HOME/test_file
       dest: $HOME/test_file_link
       state: link