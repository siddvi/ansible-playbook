---

- name: run task on node
  hosts: node
  tasks:
   - name: ensure a file exits
     copy:
       dest: "/home/{{ ansible_user }}/test_file"
       content: "{{ ansible_host }}"

   - name: fetch test_file
     fetch:
       scr: "/home/{{ ansible_user }}/test_file"
       dest: "fetched/{{ inventory_hostname }}/"
       flat: true



  