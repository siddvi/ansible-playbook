---
 - name: play 1
   hosts: node
   tasks:
     - name: execute command 'date'
       command: date

     - name: install httpd service
       yum:
          name: httpd
          state: present
     - name: start web server
       service:
          name: httpd
          state: started