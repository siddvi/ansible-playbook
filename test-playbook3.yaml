---

- name: run task on node
  hosts: node
  var:
    ansible_user: ec2-user
    env: staging
  tasks:
    - name: template file onto remote hosts
      template:
        src: my_app.conf.j2
        dest: /etc/my_app.conf
      become: true